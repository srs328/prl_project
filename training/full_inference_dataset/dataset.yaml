name: inference_dataset
data_root: /media/smbshare/srs-9/prl_project/full_inference_data
# Data identity
subjects: subjects.txt
prl_df: ${PWD}/prl_df.csv
# Fold/split config (used once to create datalist_template.json)
n_folds: 5
test_split: 1

# Preprocessing defaults (overridable by ExperimentGrid)
defaults:
  preprocessing:
    images: [flair, phase]
    expand_xy: 20
    expand_z: 2

  training:
