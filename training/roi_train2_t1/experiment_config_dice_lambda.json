{
  "train_home": "${PROJECT_ROOT}/training",
  "dataset_name": "roi_train2_t1",
  "experiment_name": "test_dicece_lambda",
  "base_label_config": "label_config.jsonc",
  "base_monai_config": "monai_config.jsonc",
  "param_grid": {
    "training": {
      "auto_scale_batch": [false],
      "batch_size": [4],
      "loss": [
        {
          "_target_": "DiceCELoss",
          "include_background": true,
          "weight": "$torch.tensor([1.0, 1.0, 5.0]).cuda()",
          "lambda_dice": 0.5,
          "lambda_ce": 1.0,
          "squared_pred": true,
          "smooth_nr": 0,
          "smooth_dr": 1.0e-05,
          "softmax": "$not @sigmoid",
          "sigmoid": "$@sigmoid",
          "to_onehot_y": "$not @sigmoid"
        }
      ]
    }
  }
}
